# Required metadata
sonar.projectKey=Bpmn
sonar.projectName=Bpmn

# Comma-separated paths to directories with sources (required)
sonar.sources=src/

# Language
sonar.language=php

# Encoding of sources files
sonar.sourceEncoding=UTF-8